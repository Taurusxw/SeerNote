# SeerNote Domain Glossary

## 条目（Entry）

用户保存的一段本地纯文本，是 SeerNote 中唯一的内容实体。条目有稳定标识、标题、正文、分类、类型和状态。

## 随手记（Memo）

类型为“随手记”的条目，用于临时想法、待办和提醒性文字。它不等于系统通知，也不承诺在指定时间弹出。

## 提示词（Prompt）

类型为“提示词”的条目，用于反复复制到其他应用。提示词可以包含模板变量。

## 模板变量（Template Variable）

提示词正文中以 `{{名称}}` 表示、复制前由用户填写的占位内容。相同名称在一次复制中只询问一次并统一替换。

## 分类（Category）

用户为条目填写的单层名称，用于筛选和搜索。分类不是可无限嵌套的文件夹树。

## 智能视图（Smart View）

由条目状态自动计算的集合，包括“全部”“随手记”“提示词”“置顶”“回收站”。智能视图不拥有条目。

## 置顶（Favorite）

让条目在列表中优先出现的可撤销状态。它与置顶小窗不同。

## 置顶小窗（Sticky Window）

绑定到一个条目的独立、始终位于普通窗口之上的编辑窗口。关闭小窗不会删除条目。

## 回收站（Trash）

保存被软删除条目的智能视图。回收站条目可还原；永久删除才不可逆。

## 权威数据（Authoritative Data）

`data/notes.json` 中最近一次成功原子提交的完整应用状态。临时文件和备份仅用于恢复，不与权威数据并行编辑。

## 便携目录（Portable Directory）

包含程序、`data/` 和备份的 SeerNote 文件夹。程序不依赖注册表中的产品设置；关闭后复制该目录即可迁移。
